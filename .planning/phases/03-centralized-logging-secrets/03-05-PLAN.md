---
phase: 03-centralized-logging-secrets
plan: 05
type: execute
wave: 1
depends_on: ["03-01", "03-03", "03-04"]
files_modified:
  - scripts/reconfigure-log-paths.ps1
  - .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md
autonomous: false
requirements: [LOG-01, SEC-01]
gap_closure: true
must_haves:
  truths:
    - "reconfigure-log-paths.ps1 no longer reports false success for BilliardBarBackend when BilliardBarNginx is running — it either stops the dependent (Nginx) first, or hard-fails instead of silently proceeding when a Stop-Service call doesn't actually stop the target service"
    - "After running the fixed script against staging, C:\\POS\\logs\\backend.log is created/actively written by a Backend process whose PID and StartTime differ from before the run, proving a real restart occurred rather than a silent no-op — no manual Stop-Service/restart workaround required"
    - "All 8 previously-DPAPI-skipped role secrets (ADMIN_PASSWORD, ADMIN_PIN, MANAGER_PASSWORD, MANAGER_PIN, WAITER1_PASSWORD, WAITER2_PASSWORD, KITCHEN_PASSWORD, BARSTAFF_PASSWORD) exist as fresh non-default values in staging's .env and have a corresponding encrypted .dat file under C:\\POS\\secrets\\ after re-running migrate-secrets-to-dpapi.ps1"
    - "reconfigure-secrets.ps1's re-run against staging lists BilliardBarBackend under Reconfigured, not Skipped, and the backend health endpoint still responds after the reconfiguration restart"
  artifacts:
    - path: "scripts/reconfigure-log-paths.ps1"
      provides: "Dependency-order-safe stop/reconfigure/start sequence for the 5 NSSM services (Nginx stopped-first/started-last relative to Backend), with a Stop-Service verification loop that hard-fails instead of silently continuing on a stuck/no-op stop"
      contains: "StopOrder"
    - path: ".planning/phases/03-centralized-logging-secrets/03-VALIDATION.md"
      provides: "Real staging evidence closing both documented Phase 3 gaps (LOG-01 dependency-ordering bug, SEC-01 Backend DPAPI skip)"
      contains: "Gap Closure Re-Validation"
  key_links:
    - from: "scripts/reconfigure-log-paths.ps1"
      to: "BilliardBarNginx / BilliardBarBackend stop-then-start ordering"
      via: "explicit $StopOrder / $StartOrder arrays processed as distinct phases before any nssm set call, with a service-status verification gate between stop and reconfigure"
      pattern: "StopOrder"
---

<objective>
Close the two documented gaps from `.planning/phases/03-centralized-logging-secrets/03-VERIFICATION.md` (status: gaps_found) that block Phase 3's full success-criteria satisfaction: (1) `reconfigure-log-paths.ps1`'s service-dependency ordering bug that silently no-ops Backend's log-path reconfiguration while Nginx is running (LOG-01), and (2) Backend's secrets remaining `.env`-sourced because staging's `.env` was missing 8 of the 16 D-07-scoped role-password/PIN secrets, correctly triggering `reconfigure-secrets.ps1`'s fail-closed skip (SEC-01).

Purpose: Plans 03-01/03-03/03-04 already proved every underlying mechanism (NSSM log redirection, DPAPI encryption, `AppEnvironmentExtra` reconfiguration) sound on real staging hardware — the two remaining issues are a real script bug and a staging-environment data gap, not a mechanism failure. This plan fixes the bug, closes the data gap using the full D-07 scope (not a Backend-specific carve-out), and re-proves both with fresh staging evidence, so Phase 3 can move from `gaps_found` to fully verified without narrowing any locked decision.

Output: a fixed `scripts/reconfigure-log-paths.ps1`, staging re-execution evidence, and an appended "Gap Closure Re-Validation" section in `03-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-centralized-logging-secrets/03-CONTEXT.md
@.planning/phases/03-centralized-logging-secrets/03-VERIFICATION.md
@.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md
@.planning/phases/03-centralized-logging-secrets/03-01-PLAN.md
@.planning/phases/03-centralized-logging-secrets/03-03-PLAN.md
@.planning/phases/03-centralized-logging-secrets/03-04-PLAN.md
@CLAUDE.md

<interfaces>
Staging machine access (CLAUDE.md "Staging machine access" section): reachable over plain OpenSSH, credentials in root `.env` (git-ignored) as `STAGING_MACHINE_IP`, `STAGING_MACHINE_USERNAME`, `STAGING_MACHINE_PASSWORD`. SSH sessions arrive already-elevated (Administrator), no UAC prompt. `STAGING_MACHINE_IP` is DHCP-assigned and can change after a sleep cycle — if a connection suddenly times out, ask the user for the current IP rather than assuming the machine is off. Command pattern: `sshpass -p "$STAGING_MACHINE_PASSWORD" ssh -o StrictHostKeyChecking=accept-new "${STAGING_MACHINE_USERNAME}@${STAGING_MACHINE_IP}" "<command>"`. Windows PowerShell 5.1 only.

Per `03-VALIDATION.md`'s recorded method: staging's checkout at `C:\Users\giris\billiards-staging` is a **file copy, not a git clone** — the fixed `scripts/reconfigure-log-paths.ps1` must be pushed to staging via `scp` (or an equivalent SSH file copy), not `git pull`.

Current `scripts/reconfigure-log-paths.ps1` (the file Task 1 fixes) processes all 5 services in a single loop — for each service: stop, set AppStdout/AppStderr/AppRotateFiles/AppRotateBytes, verify via `nssm get`, start — in the fixed order Backend, Scheduler, TelegramBot, PrintAgent, Nginx. Because Nginx (which SCM refuses to stop while its `BilliardBarBackend` dependency is needed) is processed *last*, Backend's `Stop-Service`/`nssm stop` call silently no-ops while Nginx (from the *previous* run) is still up, and the script proceeds straight to `nssm set`/`nssm start` without ever checking whether the stop actually took effect — reporting `Running` while the pre-existing Backend process (unchanged PID) is still on the old log path.

`scripts/reconfigure-secrets.ps1`'s `BilliardBarBackend` block (already correct, do not modify) requires all of: `SECRET_KEY`, `JWT_REFRESH_SECRET`, `ADMIN_PASSWORD`, `ADMIN_PIN`, `MANAGER_PASSWORD`, `MANAGER_PIN`, `WAITER1_PASSWORD`, `WAITER2_PASSWORD`, `KITCHEN_PASSWORD`, `BARSTAFF_PASSWORD`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD` to be present as `.dat` files under `C:\POS\secrets\` before it will reconfigure `BilliardBarBackend`; if any one is missing it skips the whole service rather than partially applying (fail-closed, by design — do not weaken this guard). `scripts/migrate-secrets-to-dpapi.ps1` (already correct, do not modify) is safe to re-run — it overwrites each key's `.dat` file with whatever is currently in `.env`, and silently skips (with a yellow "SKIPPED" line) any key not present/non-empty in `.env`.

Code-review finding (confirmed via `grep -rn ADMIN_PASSWORD backend/`): the running Flask app reads `ADMIN_PASSWORD`/`ADMIN_PIN`/etc. from `os.environ` in exactly one runtime place — `backend/app/__init__.py`'s `_check_default_secrets()` (D-12's warn-only default-value check, around line 1062-1075) — purely to compare against known-insecure defaults and log a warning; it never uses the value to authenticate a request. The only place these values set actual login credentials is `backend/seed.py` (`admin.set_password(os.environ.get('ADMIN_PASSWORD', 'admin123'))`), which already ran once at initial staging seed time using whatever default/`.env` value existed *then*. Seeding a **fresh** value for these 8 keys into `.env` now (to satisfy `reconfigure-secrets.ps1`'s fail-closed guard and complete the D-07-scoped DPAPI migration) does not change the already-hashed admin/manager/waiter/kitchen/barstaff passwords stored in the database, and does not risk locking out any existing staging login — this is purely about closing the DPAPI-migration data gap, matching D-07's full 16-key scope rather than carving out a Backend-specific exception.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix reconfigure-log-paths.ps1's service-dependency ordering bug</name>
  <files>scripts/reconfigure-log-paths.ps1</files>
  <read_first>
    scripts/reconfigure-log-paths.ps1 (current 141-line file — the single per-service stop/set/verify/start loop this task restructures)
    .planning/phases/03-centralized-logging-secrets/03-VERIFICATION.md (Gap #1 section — exact root cause and both suggested fix paths)
    .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md (LOG-01 section — the manual workaround sequence that proves the underlying mechanism, which this fix must reproduce automatically: Stop-Service BilliardBarNginx -> nssm restart BilliardBarBackend -> Start-Service BilliardBarNginx)
  </read_first>
  <action>
    Restructure the script's single "for each service: stop -> set -> verify -> start" loop into three explicit, dependency-ordered phases, keeping every existing behavior (log directory creation, ACL restriction via icacls, NSSM auto-detection fallback chain, the 5-service definitions with their Stdout/Stderr/RotateBytes values) unchanged.
    Define `$StopOrder` as an ordered list of the 5 service names with `BilliardBarNginx` FIRST (it depends on `BilliardBarBackend`, so Windows SCM will refuse to stop Backend while Nginx still needs it — Nginx must release that dependency before Backend's stop can succeed), followed by `BilliardBarBackend`, `BilliardBarScheduler`, `BilliardBarTelegramBot`, `BilliardBarPrintAgent` in any order. Define `$StartOrder` as the same 5 names with `BilliardBarNginx` LAST (so its dependency, Backend, is already running again before Nginx starts).
    Phase A (stop): iterate `$StopOrder`; for each installed service, call the existing `nssm stop <name> confirm`, then poll `(Get-Service -Name <name>).Status` up to 5 times with a 1-second sleep between polls until it reports `Stopped`. If it never reaches `Stopped` after all retries, this is the exact bug this task fixes — do not proceed to `nssm set`/`nssm start` for that service; print a red error naming the stuck service and `exit 1` immediately (this is the "check Stop-Service actually succeeded" half of the fix — it converts today's silent false-success into a loud, immediate failure instead of a same-old-process no-op).
    Phase B (reconfigure): iterate the same 5 services (order no longer matters — every service that reached `Stopped` in Phase A is confirmed stopped); apply the existing `nssm set AppStdout`/`AppStderr`/`AppRotateFiles`/`AppRotateBytes` logic and the existing `nssm get`-based verification exactly as today, unchanged.
    Phase C (start): iterate `$StartOrder`; for each service, call the existing `nssm start <name>`, then poll `Get-Service` status until `Running` (same retry-with-sleep pattern as Phase A, but checking for `Running` instead of `Stopped`) before moving to the next service in the list — this guarantees Backend (and the other 3) are fully up before Nginx's turn, so Nginx's dependency is satisfied the moment it starts.
    Preserve the existing per-service skip-if-not-installed check, the existing `$summary` table (OldPath/NewPath), and the final "Reconfiguration Summary"/"Run .\scripts\tail-logs.ps1 next" output exactly as today — only the stop/reconfigure/start control flow changes.
  </action>
  <acceptance_criteria>
    - `$StopOrder`'s first element is `"BilliardBarNginx"`; `$StartOrder`'s last element is `"BilliardBarNginx"`
    - Every `nssm stop`/`Stop-Service` call is followed by a poll loop checking `Status -eq 'Stopped'` (or equivalent) before that service's `nssm set` calls run
    - A service that fails to reach `Stopped` after the retry budget causes the script to `exit 1` with a message naming that specific service — it does not silently continue to `nssm set`/`nssm start`
    - Every `nssm start` call is followed by a poll loop checking for `Running` before the next service in `$StartOrder` is started
    - AppRotateFiles/AppRotateBytes values (10485760 for backend/scheduler/bot/nginx, 1048576 for print-agent) are unchanged from the current file
    - `#Requires -RunAsAdministrator`, the `C:\POS\logs` ACL-restriction block, and the NSSM auto-detection fallback chain are unchanged
  </acceptance_criteria>
  <verify>
    <automated>for p in "StopOrder" "StartOrder" "BilliardBarNginx" "Stopped" "exit 1" "RunAsAdministrator" "icacls"; do grep -qE "$p" scripts/reconfigure-log-paths.ps1 || echo "MISSING:$p"; done | grep -c MISSING | grep -q "^0$" && echo PASS</automated>
  </verify>
  <done>scripts/reconfigure-log-paths.ps1 stops Nginx before Backend (or hard-fails on a stuck stop), reconfigures all 5 services only once genuinely stopped, and starts Backend/Scheduler/TelegramBot/PrintAgent before Nginx — eliminating the silent false-success bug documented in 03-VERIFICATION.md's Gap #1.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Re-run the fixed script and complete the DPAPI secrets migration on staging, capturing real evidence</name>
  <files>N/A — this task pushes the Task 1 fix to the staging Windows machine and runs scripts there over SSH; it does not modify any file in this repository checkout directly (Task 3 records the results)</files>
  <read_first>
    .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md (exact staging path C:\Users\giris\billiards-staging, and the scp-based file-push method used since staging is not a git clone)
    .planning/phases/03-centralized-logging-secrets/03-04-PLAN.md Task 1 (the established command sequence/evidence-capture pattern this task extends: Get-Service, Get-ChildItem, icacls, health-check, log tail)
    scripts/reconfigure-secrets.ps1 (the BilliardBarBackend secretKeys list at lines 176-179 — the exact 14 keys that must all resolve for Backend to leave the Skipped list)
  </read_first>
  <action>
    Source `STAGING_MACHINE_IP`/`STAGING_MACHINE_USERNAME`/`STAGING_MACHINE_PASSWORD` from the repo-root `.env` (never echo the password). Confirm connectivity with a trivial remote command (`whoami`); if it times out (not an auth error), ask the user to confirm the current `STAGING_MACHINE_IP` before proceeding.
    Push Task 1's fixed `scripts/reconfigure-log-paths.ps1` to `C:\Users\giris\billiards-staging\scripts\reconfigure-log-paths.ps1` via `scp` (staging is a file copy, not a git clone — do not attempt `git pull` there).
    Capture a baseline: `Get-Process -Name pythonw,python -ErrorAction SilentlyContinue | Select-Object Id,StartTime,Path` (or the equivalent process listing for the Backend service) before running anything, so the post-run PID/StartTime comparison in the next step has something to diff against.
    Run the fixed `.\scripts\reconfigure-log-paths.ps1` as Administrator. Capture full stdout/stderr. Then capture: `Get-Service BilliardBar*` (all installed services should show Running, including Nginx), the same `Get-Process` query as the baseline (compare PID/StartTime against the baseline to prove a real restart happened, not a no-op), `Get-ChildItem C:\POS\logs\backend.log,C:\POS\logs\backend_err.log | Select-Object Name,Length,LastWriteTime`, and the last 10 lines of `C:\POS\logs\backend.log` (to confirm it is actively being written, not stale/empty). No manual Stop-Service/restart workaround should be necessary this time.
    Check which of the 8 previously-skipped role secrets are present in staging's `.env` via a single remote command that only reports key NAMES found (e.g. matching `^(ADMIN_PASSWORD|ADMIN_PIN|MANAGER_PASSWORD|MANAGER_PIN|WAITER1_PASSWORD|WAITER2_PASSWORD|KITCHEN_PASSWORD|BARSTAFF_PASSWORD)=` and extracting only the portion before `=`) — never let a matched line's value reach the transcript.
    In one remote PowerShell invocation (so no intermediate value is ever echoed back over SSH), for every one of those 8 keys NOT already present/non-empty in `.env`: generate a fresh random value server-side (24-character alphanumeric for the 6 `*_PASSWORD` keys via a `Get-Random`-derived character sequence; a 4-digit number in the 1000-9999 range for `ADMIN_PIN`/`MANAGER_PIN`) and append `KEY=value` to `.env` with `Add-Content`. The command must print only the list of key NAMES it added (or "already present" for keys it skipped) — never the generated values.
    Re-run `.\scripts\migrate-secrets-to-dpapi.ps1` (safe to re-run — overwrites each key's `.dat` file). Capture its "Migrated (X/16)"/"Skipped" summary; confirm none of the 8 role-secret keys still appear in the Skipped list.
    Re-run `.\scripts\reconfigure-secrets.ps1`. Capture its "Reconfigured (Y/3)"/"Skipped" summary; confirm `BilliardBarBackend` now appears under Reconfigured, not Skipped.
    Confirm, without ever printing the full `AppEnvironmentExtra` value (it contains plaintext secrets by design per D-06): run a boolean-only check such as `(& $NssmExe get BilliardBarBackend AppEnvironmentExtra) -match 'ADMIN_PASSWORD=' -and (& $NssmExe get BilliardBarBackend AppEnvironmentExtra) -match 'DATABASE_URL='` and capture only the resulting `True`/`False`.
    Capture `Get-ChildItem C:\POS\secrets\ | Measure-Object | Select-Object -ExpandProperty Count` (expect 16, or the exact count of D-07 keys now present in `.env`) and `Invoke-RestMethod http://localhost:5000/api/v1/auth/me -TimeoutSec 5` (any HTTP response, including 401, counts as healthy) after the reconfiguration restart.
  </action>
  <what-built>
    A dependency-order-safe `reconfigure-log-paths.ps1` (Task 1) and a completed DPAPI migration for the 8 previously-missing role secrets, closing both documented Phase 3 gaps.
  </what-built>
  <how-to-verify>
    Run the sequence in this task's action against the staging machine (WIDOWSVAIL) as Administrator over SSH. Report back: (a) full reconfigure-log-paths.ps1 stdout/stderr, (b) Get-Service BilliardBar* output, (c) the before/after Get-Process PID/StartTime comparison for Backend, (d) Get-ChildItem + tail output for backend.log, (e) the list of role-secret key names added to .env (names only), (f) migrate-secrets-to-dpapi.ps1's migrated/skipped summary, (g) reconfigure-secrets.ps1's reconfigured/skipped summary showing Backend reconfigured, (h) the True/False AppEnvironmentExtra marker-check result, (i) the C:\POS\secrets\ file count, (j) the backend health-check HTTP result.
  </how-to-verify>
  <acceptance_criteria>
    - reconfigure-log-paths.ps1 was re-run on the actual staging machine with no manual Stop-Service/restart workaround needed, and Backend's post-run PID/StartTime differ from the pre-run baseline
    - C:\POS\logs\backend.log exists with a LastWriteTime after the script ran, and its tail shows real recent content
    - Get-Service BilliardBar* shows Nginx (and every other installed service) Running after the script completes
    - All 8 previously-missing role-secret key names were confirmed added to staging's .env without any generated value appearing in captured output
    - migrate-secrets-to-dpapi.ps1's re-run summary shows none of the 8 role-secret keys in its Skipped list
    - reconfigure-secrets.ps1's re-run summary lists BilliardBarBackend under Reconfigured, not Skipped
    - The AppEnvironmentExtra marker-check reports True, and the backend health check returns an HTTP response (not a connection error) after the restart
    - No captured/reported output at any point contains a plaintext secret value
  </acceptance_criteria>
  <verify>
    <human-check>Concrete command output matching every item in acceptance_criteria has been reported back — not a bare "it worked" confirmation. If a step genuinely could not complete (unreachable/changed STAGING_MACHINE_IP, a service still failing to reach Running after the retry budget, or a still-missing secret), the specific failing step and captured error are reported instead, and the gap remains open for a follow-up plan.</human-check>
  </verify>
  <done>The fixed reconfigure-log-paths.ps1 has been proven on staging to consolidate Backend's logs without a manual workaround, and BilliardBarBackend has been proven to be fully DPAPI-migrated (Reconfigured, not Skipped) after seeding the 8 previously-missing role secrets.</done>
  <resume-signal>Paste the requested command output above, or describe what failed and at which numbered step.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record gap closure evidence in 03-VALIDATION.md</name>
  <files>.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md</files>
  <read_first>
    .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md (existing LOG-01/SEC-01 sections this task appends to, not replaces)
    .planning/phases/03-centralized-logging-secrets/03-VERIFICATION.md (Gaps Summary section — the exact two gaps this evidence must close)
  </read_first>
  <action>
    Append a new `## Gap Closure Re-Validation (Plan 03-05)` section to the end of `.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md` (do not edit or remove the existing LOG-01/SEC-01 sections from the original 03-04 validation — they remain the historical record of what was first found).
    Under a `### LOG-01 — dependency-ordering bug closed` subsection, cite Task 2's captured evidence: the before/after PID/StartTime diff for Backend, confirmation that no manual workaround was needed this run, and the Get-Service BilliardBar* output showing Nginx Running afterward.
    Under a `### SEC-01 — Backend DPAPI migration completed` subsection, cite Task 2's captured evidence: which of the 8 role-secret keys were newly seeded (names only, never values), migrate-secrets-to-dpapi.ps1's updated migrated count, reconfigure-secrets.ps1's Reconfigured list now including BilliardBarBackend, the AppEnvironmentExtra marker-check True result, and the post-restart health-check response. Explicitly state the code-review finding that `ADMIN_PASSWORD`'s only runtime consumer in the Flask app is the D-12 default-value warning check (not authentication), so seeding a fresh value did not affect the already-seeded admin login.
    If Task 2's action could not be fully completed (a step failed and was reported as a failure), record that honestly as still-open rather than marking it closed — do not overwrite a real failure with an optimistic summary.
    Add one closing sentence noting that the next `/gsd:verify-phase 03` run should re-derive LOG-01/SEC-01's status from this evidence.
  </action>
  <acceptance_criteria>
    - 03-VALIDATION.md contains a `## Gap Closure Re-Validation (Plan 03-05)` heading with both an LOG-01 and a SEC-01 subsection
    - Every claim in the new section cites specific evidence captured in Task 2 (command output, counts, True/False results) rather than asserting success without evidence
    - The original 03-04 LOG-01/SEC-01 sections are unmodified (still present above the new section)
    - No plaintext secret value appears anywhere in the new section
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Gap Closure Re-Validation" .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md && grep -q "LOG-01" .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md && grep -q "SEC-01" .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md && echo PASS</automated>
  </verify>
  <done>.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md documents real staging evidence closing both Phase 3 gaps, appended after the original 03-04 record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Claude's SSH session to the staging machine | Arrives pre-elevated as Administrator — every command in this plan runs at the highest privilege level available on that machine |
| Generated role-secret values -> staging `.env` -> DPAPI store | Freshly generated random test values, never transmitted back into this repo or any captured transcript |
| Task 2's captured SSH output -> `03-VALIDATION.md` | Captured command output is trusted as-is when writing the evidence record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Information Disclosure | Freshly generated role-secret values (ADMIN_PASSWORD etc.) could leak if echoed in SSH command output or copied into 03-VALIDATION.md | mitigate | Task 2's action explicitly generates and appends values within a single remote invocation that only ever prints key NAMES back over the SSH channel; Task 3's acceptance criteria forbid any plaintext secret value in the recorded evidence. |
| T-03-18 | Tampering | reconfigure-log-paths.ps1's new Stop-Service verification loop could still race if a service takes longer than the retry budget to stop, causing a false failure (not a security issue, but a reliability one that could mask the real fix) | accept | The retry budget (5 polls x 1s) mirrors the existing script's own `Start-Sleep -Seconds 2/3` conventions; Task 2's live staging run is the authoritative proof this works in practice, not just the automated grep check. |
| T-03-19 | Elevation of Privilege | Re-running Administrator-scoped scripts against newly-generated role credentials, even on disposable staging | accept | Staging is an explicitly disposable test environment per CLAUDE.md's "Staging machine access" section; generated secrets are test-only values, not real bar-machine credentials — Phase 5 will generate fresh production secrets per D-09's already-flagged precedent, not carry these over. |
| T-03-20 | Repudiation | 03-VALIDATION.md's new section records SSH-captured output verbatim, with no independent second observer | accept | Same accepted limitation as the original 03-04 validation (T-02-19/T-03-15 precedent) — inherent to validating a Windows-only deployment target from this macOS environment; Task 2's checkpoint requires concrete command output, not a bare confirmation. |

Severity note: No HIGH-severity threat is newly introduced — this plan closes an existing MEDIUM-severity gap (per 03-VERIFICATION.md) rather than expanding attack surface; the one Information Disclosure risk (T-03-17) is directly mitigated by never letting generated secret values leave the remote SSH session's server-side execution.
</threat_model>

<verification>
Task 1's automated grep confirms the structural fix is present. Task 2 requires real staging-machine command output proving both gaps are closed (no manual workaround needed for LOG-01; BilliardBarBackend reconfigured, not skipped, for SEC-01) — do not mark this plan complete until that evidence is captured. Task 3's automated grep confirms 03-VALIDATION.md was updated with that evidence.
</verification>

<success_criteria>
- `scripts/reconfigure-log-paths.ps1` respects Nginx's service dependency on Backend (stops Nginx first, starts it last) and hard-fails instead of silently proceeding on a stuck Stop-Service call.
- Re-running the fixed script on staging consolidates Backend's logs into `C:\POS\logs\backend.log` without any manual Stop-Service/restart workaround.
- All 16 D-07-scoped secrets present in staging's `.env` (including the 8 role secrets seeded by this plan) are DPAPI-encrypted under `C:\POS\secrets\`.
- `reconfigure-secrets.ps1` reconfigures `BilliardBarBackend` (no longer skipped) to source its secrets from the DPAPI store, and the backend remains healthy afterward.
- `.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md` records real evidence closing both gaps from `03-VERIFICATION.md`.
</success_criteria>

<output>
Create `.planning/phases/03-centralized-logging-secrets/03-05-SUMMARY.md` when done
</output>
</content>
