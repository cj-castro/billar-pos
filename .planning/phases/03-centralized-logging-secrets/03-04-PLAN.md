---
phase: 03-centralized-logging-secrets
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md
autonomous: false
requirements: [LOG-01, LOG-02, LOG-03, SEC-01, SEC-02]
must_haves:
  truths:
    - "All new/modified Phase 3 scripts were actually executed on the staging Windows machine (WIDOWSVAIL), not just reviewed — this environment has no Windows/NSSM/PowerShell runtime to test them locally"
    - "After execution, every one of BilliardBarBackend/Scheduler/TelegramBot/PrintAgent/Nginx is confirmed Running and writing to C:\\POS\\logs\\, with nginx's own access/error logs also present there"
    - "After execution, C:\\POS\\secrets\\ contains one .dat file per D-07 secret that was present in staging's .env, and the three secret-consuming services are confirmed still functioning (backend health endpoint responds) after AppEnvironmentExtra was repointed to the DPAPI store"
    - "The backend's insecure-default-secret warning is confirmed to actually fire in the real service logs when a default value is present (or confirmed absent when all staging secrets are already non-default), not just reviewed as code"
    - "03-VALIDATION.md records real evidence for LOG-01, LOG-02, LOG-03, SEC-01, SEC-02 — not placeholder text"
  artifacts:
    - path: ".planning/phases/03-centralized-logging-secrets/03-VALIDATION.md"
      provides: "Recorded staging-machine evidence for every Phase 3 requirement"
      contains: "LOG-01"
  key_links:
    - from: ".planning/phases/03-centralized-logging-secrets/03-VALIDATION.md"
      to: "ROADMAP.md Phase 3 Success Criteria"
      via: "one recorded PASS/FAIL/PARTIAL line per requirement ID, sourced from real command output captured over SSH"
      pattern: "LOG-0[1-3]|SEC-0[12]"
---

<objective>
Actually run every script produced in Plans 03-01/03-02/03-03 on the staging Windows machine over SSH, and record real evidence (not static review) that Phase 3's goal — "an operator can observe all service activity from one place, and secrets no longer live in committed files" — is genuinely true.

Purpose: Plans 03-01/03-02/03-03 each produced isolated, unexecuted PowerShell/Python artifacts (this macOS environment cannot run NSSM/PowerShell/Windows Credential Manager). This plan is where those artifacts are actually run together on real hardware, mirroring Phase 2's 02-05 pattern, and where the phase's requirements are proven true or found lacking.

Output: `.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-centralized-logging-secrets/03-CONTEXT.md
@.planning/phases/03-centralized-logging-secrets/03-01-PLAN.md
@.planning/phases/03-centralized-logging-secrets/03-02-PLAN.md
@.planning/phases/03-centralized-logging-secrets/03-03-PLAN.md
@CLAUDE.md

<interfaces>
Staging machine access (CLAUDE.md "Staging machine access" section): reachable over plain OpenSSH, credentials in root `.env` (git-ignored) as `STAGING_MACHINE_IP`, `STAGING_MACHINE_USERNAME`, `STAGING_MACHINE_PASSWORD`. SSH sessions arrive already-elevated (Administrator), no UAC prompt. `STAGING_MACHINE_IP` is DHCP-assigned and can change after a sleep cycle — if connection suddenly times out, ask the user for the current IP rather than assuming the machine is off. Command pattern: `sshpass -p "$STAGING_MACHINE_PASSWORD" ssh -o StrictHostKeyChecking=accept-new "${STAGING_MACHINE_USERNAME}@${STAGING_MACHINE_IP}" "<command>"`. Windows PowerShell 5.1 only.

02-05-SUMMARY.md documents a Windows "Session 0" limitation that broke GUI-capable installer bootstraps (Postgres/npm installers) when driven over plain SSH, worked around via a remotely-triggered Scheduled Task running in the interactive session. Phase 3's scripts install no new software and launch no GUI installers (only `nssm set`/`icacls`/file moves/DPAPI calls) — plain SSH invocation is expected to work directly; fall back to the same Scheduled-Task-trigger workaround only if a script actually hangs or silently no-ops over SSH. This plan reused Phase 2's proven execution channel (Claude driving SSH directly, per 02-05-SUMMARY.md's "ran the checkpoint remotely over SSH ... per explicit user direction to keep the flow automated end-to-end") rather than requiring the user to touch the machine physically — hence `type="checkpoint:human-action"` here covers the genuine automation-boundary (no local Windows runtime), not a request for manual user labor.

Repo must already be pulled/updated on the staging machine's checkout (same working copy Phase 2 validated against) before running these scripts — confirm the five new/modified files from Plans 03-01/02/03 are present there first (git pull or manual sync).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Execute all Phase 3 scripts on the staging machine and capture real output</name>
  <files>N/A — this task runs scripts on the staging Windows machine over SSH; it does not modify any file in this repository checkout</files>
  <read_first>
    .planning/phases/03-centralized-logging-secrets/03-01-PLAN.md (exact scripts/artifacts Task 1 produced: reconfigure-log-paths.ps1, nginx configs, rotate-nginx-logs.ps1, tail-logs.ps1)
    .planning/phases/03-centralized-logging-secrets/03-03-PLAN.md (exact scripts produced: migrate-secrets-to-dpapi.ps1, reconfigure-secrets.ps1, and their required run order — migrate before reconfigure)
  </read_first>
  <action>
    Source `STAGING_MACHINE_IP`/`STAGING_MACHINE_USERNAME`/`STAGING_MACHINE_PASSWORD` from the repo-root `.env` (never echo the password). Confirm connectivity with a trivial remote command first (`whoami`); if it fails with a timeout (not an auth error), ask the user to confirm the current `STAGING_MACHINE_IP` before proceeding (DHCP may have changed it).
    Ensure the staging machine's repo checkout has this branch's latest commits, including this phase's five new/modified files (`git pull` or equivalent remote step) plus a working `.env` with values for the 16 D-07 secret keys — if the remote `.env` is missing several of those keys, note which ones rather than blocking (migrate-secrets-to-dpapi.ps1's own skip-and-warn behavior handles this).
    Run, in this exact order, each as its own remote PowerShell invocation (`Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` once per session is sufficient), capturing full stdout/stderr for each: (1) `.\scripts\reconfigure-log-paths.ps1`, (2) copy the updated `scripts\nginx-windows.conf` over `C:\nginx\conf\nginx.conf` and `nssm restart BilliardBarNginx` (deploys this phase's D-02 config changes onto the already-installed nginx — not a re-run of install-nssm-nginx.ps1), (3) `.\scripts\rotate-nginx-logs.ps1 -Register`, (4) `.\scripts\migrate-secrets-to-dpapi.ps1`, (5) `.\scripts\reconfigure-secrets.ps1`.
    After all five complete, capture: `Get-Service BilliardBar*` (all should show Running), `Get-ChildItem C:\POS\logs\` (should list backend/scheduler/telegram_bot/print_agent/nginx_service/nginx_access/nginx_error log files), `Get-ChildItem C:\POS\secrets\` (should list one .dat per secret actually present in remote .env), `icacls C:\POS\logs` and `icacls C:\POS\secrets` (confirm restricted to Administrators/SYSTEM only), `Invoke-RestMethod http://localhost:5000/api/v1/auth/me -TimeoutSec 5` (any HTTP response counts as backend still healthy post-secrets-reconfiguration), and the last 20 lines of `C:\POS\logs\backend_err.log` / `backend.log` (to directly observe whether the D-11/D-12 insecure-default-secret warning fired, and confirm no unrelated crash occurred).
    Also run `.\scripts\tail-logs.ps1` for a short bounded window (e.g. background job with an 8-second timeout, or redirect to a file and read it back after a few seconds) to confirm it actually emits merged, service-prefixed lines rather than just reviewing the script text.
  </action>
  <what-built>
    Four new/modified PowerShell scripts (reconfigure-log-paths.ps1, rotate-nginx-logs.ps1, migrate-secrets-to-dpapi.ps1, reconfigure-secrets.ps1), two updated nginx configs, and a backend code change (_check_default_secrets) — together consolidating all Phase 2 service logs into C:\POS\logs\ and moving secrets from .env into a DPAPI-encrypted store under C:\POS\secrets\.
  </what-built>
  <how-to-verify>
    Run the five scripts in the documented order on the staging machine (192.168.1.x, WIDOWSVAIL) as Administrator over SSH, per this task's action. Report back: (a) full Get-Service BilliardBar* output, (b) full Get-ChildItem listings for C:\POS\logs\ and C:\POS\secrets\, (c) icacls output for both directories, (d) the backend health-check HTTP result, (e) the last 20 lines of backend.log/backend_err.log, (f) a short captured sample of tail-logs.ps1's live output, (g) any error output if a step failed.
  </how-to-verify>
  <acceptance_criteria>
    - All five scripts were invoked on the actual staging machine and their real stdout/stderr was captured (not simulated/assumed)
    - `Get-Service BilliardBar*` output shows all 5 services Running after every script completed
    - `Get-ChildItem C:\POS\logs\` output includes at least: backend.log, scheduler.log, telegram_bot.log, print_agent.log, nginx_service.log, nginx_access.log, nginx_error.log
    - `Get-ChildItem C:\POS\secrets\` output includes a .dat file for every secret key that was actually present in the remote .env
    - `icacls` output for both C:\POS\logs and C:\POS\secrets shows no broader-than-Administrators/SYSTEM grant
    - Backend health check (`/api/v1/auth/me`) returns an HTTP response (not a connection error) after reconfigure-secrets.ps1 ran
    - tail-logs.ps1's captured output contains at least one timestamped, service-prefixed line
  </acceptance_criteria>
  <verify>
    <human-check>Concrete command output matching every item in acceptance_criteria has been reported back (Get-Service, Get-ChildItem x2, icacls x2, health-check result, log tail, tail-logs.ps1 sample) — not a bare "it worked" confirmation. If a step genuinely could not complete (persistent Session 0 hang even after the Scheduled-Task workaround, or an unreachable/changed STAGING_MACHINE_IP), the specific failing step and captured error are reported instead.</human-check>
  </verify>
  <done>Every Phase 3 script has been executed on the real staging machine, all 5 services are confirmed Running, C:\POS\logs\ and C:\POS\secrets\ contain the expected ACL-restricted files, the backend remains healthy after secrets reconfiguration, and tail-logs.ps1 was proven to emit real merged output.</done>
  <resume-signal>Paste the requested command output above, or describe what failed and at which numbered step.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record validation results against every Phase 3 requirement</name>
  <files>.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 3's four Success Criteria — the exact claims this document must address)
    .planning/phases/03-centralized-logging-secrets/03-CONTEXT.md (D-11's exact "warn, never fail" wording — must be stated precisely against whatever Task 1 actually observed)
  </read_first>
  <action>
    Using Task 1's captured output, write .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md with one section per requirement ID (LOG-01, LOG-02, LOG-03, SEC-01, SEC-02) stating PASS/FAIL/PARTIAL and the specific evidence line from Task 1's output that supports it — e.g. "LOG-01: PASS — Get-ChildItem C:\POS\logs\ listed backend.log, scheduler.log, telegram_bot.log, print_agent.log, nginx_service.log, nginx_access.log, nginx_error.log, all with recent LastWriteTime." For SEC-01, explicitly state whether the D-06-accepted limitation (secrets still plaintext-in-memory via AppEnvironmentExtra) was verified as the ONLY remaining plaintext exposure (i.e. the on-disk .env is no longer the operative source for the 3 reconfigured services). For LOG-02, cite the actual AppRotateBytes values confirmed via `nssm get` (or the icacls/file-listing evidence) rather than asserting rotation works from code review alone.
    If Task 1's captured output shows any FAIL (a service not Running, a missing log file, a health-check failure, an ACL broader than expected), record that requirement as FAIL with the specific reported error — do not mark it passing so the phase's separate `/gsd:verify-phase` step can surface it as a gap.
    Record, as a scope confirmation line, that all Phase 3 execution happened on the staging machine only (same scope boundary as Phase 2), and that no bar-machine deployment occurred.
  </action>
  <acceptance_criteria>
    - .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md exists and contains a section for LOG-01, LOG-02, LOG-03, SEC-01, and SEC-02
    - Each section's PASS/FAIL/PARTIAL status is backed by a specific piece of evidence quoted or paraphrased from Task 1's captured output, not asserted without evidence
    - Any requirement whose Task 1 evidence showed a failure is recorded as FAIL, not silently marked PASS
    - Document explicitly states this validation ran on the staging machine only
  </acceptance_criteria>
  <verify>
    <automated>for id in "LOG-01" "LOG-02" "LOG-03" "SEC-01" "SEC-02"; do grep -q "$id" .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md || echo "MISSING:$id"; done | grep -c MISSING | grep -q "^0$" && echo PASS</automated>
  </verify>
  <done>.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md exists and documents real staging-machine evidence for every Phase 3 requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Operator's/Claude's SSH session to the staging machine | Arrives pre-elevated as Administrator — every command in this plan runs at the highest privilege level available on that machine |
| This plan's checkpoint output ↔ 03-VALIDATION.md | Captured SSH command output is trusted as-is when writing the validation record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Elevation of Privilege | Running five separate scripts as Administrator, in sequence, over an automated SSH session | mitigate | Each script was independently reviewed under its own plan's threat model (03-01/03-03) before being run here; Task 1 captures real output at every step rather than assuming success, so a failure is caught immediately rather than compounding into the next script. |
| T-03-15 | Repudiation | 03-VALIDATION.md records SSH-captured output verbatim, with no independent second observer | accept | Same accepted limitation as Phase 2's 02-VALIDATION.md (T-02-19) — inherent to validating a Windows-only deployment target from a macOS planning/execution environment; the checkpoint requires concrete command output, not a bare confirmation. |
| T-03-16 | Information Disclosure | SSH session output could incidentally include a plaintext secret if a script had a printing bug missed by 03-03's static review | mitigate | Task 1's acceptance criteria only ask for `Get-ChildItem`/`icacls`/`Get-Service` output and log tails — never a raw dump of `.env` or a decrypted secret value; if any captured output unexpectedly contains a secret-looking string, that is itself a FAIL to record against SEC-01 in Task 2, not something to silently omit. |

Severity note: No HIGH-severity threat is introduced beyond what Plans 03-01/03-03 already addressed individually; this plan's own risk is procedural (verifying real execution), not a new attack surface.
</threat_model>

<verification>
Task 1 requires real staging-machine command output; do not mark this plan complete until 03-VALIDATION.md exists and contains real evidence (not placeholder text) for LOG-01, LOG-02, LOG-03, SEC-01, and SEC-02.
</verification>

<success_criteria>
- Every Phase 3 script has actually been run on the staging machine, with all 5 services confirmed Running afterward.
- C:\POS\logs\ and C:\POS\secrets\ both exist, contain the expected files, and are ACL-restricted to Administrators/SYSTEM.
- The backend remains healthy after its AppEnvironmentExtra was repointed to the DPAPI secret store.
- `.planning/phases/03-centralized-logging-secrets/03-VALIDATION.md` records a PASS/FAIL/PARTIAL verdict with real evidence for every one of LOG-01, LOG-02, LOG-03, SEC-01, SEC-02.
</success_criteria>

<output>
Create `.planning/phases/03-centralized-logging-secrets/03-04-SUMMARY.md` when done
</output>
</output>
