---
phase: 04-process-supervision-reliability-hardening
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - .planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md
  - scripts/install-nssm-telegram-bot.ps1
  - telegram-bot/bot.py
autonomous: false
requirements: [NET-02, SUP-04, DATA-02, DATA-03, SUP-01]

must_haves:
  truths:
    - "Every file Plans 04-01/04-02/04-03 produced was actually deployed and exercised on the real staging Windows machine, not just reviewed as static code"
    - "The backend's deepened health endpoint, print-agent warning, and ghost-ticket triggers all behave correctly against a live Postgres and live backend process"
    - "BilliardBarPrintAgent is installed and Running on staging for the first time, and BilliardBarTelegramBot's pre-existing crash-loop (documented in 03-VALIDATION.md) is either fixed with a confirmed root cause, or left broken with concrete diagnostic evidence recorded — never silently claimed fixed"
  artifacts:
    - path: ".planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md"
      provides: "Real staging evidence for NET-02, SUP-04, DATA-02, DATA-03, and partial SUP-01"
      contains: "NET-02"
  key_links:
    - from: ".planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md"
      to: "ROADMAP.md Phase 4 Success Criteria"
      via: "one PASS/FAIL/PARTIAL line per requirement, sourced from real staging command output"
      pattern: "NET-02|SUP-04|DATA-02|DATA-03"
---

<objective>
Deploy every file Plans 04-01/04-02/04-03 produced to the staging Windows machine (WIDOWSVAIL) over SSH, install the print agent there for the first time, diagnose and fix (or accurately document) the pre-existing `BilliardBarTelegramBot` crash-loop, apply the new Postgres failure-recovery policy, and record real evidence that NET-02, SUP-04, DATA-02, and DATA-03 are genuinely true — not just that the code compiles.

Purpose: Plans 04-01/04-02/04-03 each produced isolated, unexecuted artifacts (this macOS environment cannot run Flask/eventlet/NSSM/PowerShell/Postgres). This is where those artifacts actually run together on real hardware, mirroring Phase 2's 02-05 and Phase 3's 03-04 precedent, and where four of Phase 4's seven requirements are proven true or found lacking — before Plan 04-05's live crash/reboot tests build on this deployed state.

Output: `.planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md` (partial — SUP-01 full, SUP-02, SUP-03 continue in Plan 04-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-process-supervision-reliability-hardening/04-CONTEXT.md
@.planning/phases/04-process-supervision-reliability-hardening/04-01-PLAN.md
@.planning/phases/04-process-supervision-reliability-hardening/04-02-PLAN.md
@.planning/phases/04-process-supervision-reliability-hardening/04-03-PLAN.md
@CLAUDE.md

<interfaces>
Staging machine access (`CLAUDE.md` "Staging machine access" section): reachable over plain OpenSSH, credentials in root `.env` (git-ignored) as `STAGING_MACHINE_IP`, `STAGING_MACHINE_USERNAME`, `STAGING_MACHINE_PASSWORD`. SSH sessions arrive already-elevated (Administrator), no UAC prompt. `STAGING_MACHINE_IP` is DHCP-assigned and can change after a sleep cycle — if a connection suddenly times out, ask the user for the current IP rather than assuming the machine is off. Command pattern: `sshpass -p "$STAGING_MACHINE_PASSWORD" ssh -o StrictHostKeyChecking=accept-new "${STAGING_MACHINE_USERNAME}@${STAGING_MACHINE_IP}" "<command>"`. Windows PowerShell 5.1 only.

Per `03-VALIDATION.md` (Phase 3's recorded method): staging's checkout at `C:\Users\giris\billiards-staging` (machine `WIDOWSVAIL`, most recently observed at `192.168.1.18` — reconfirm current IP before use) is a **file copy, not a git clone** — push files via `scp`, never `git pull`. As of Phase 3's gap-closure validation (`03-VALIDATION.md` "Gap Closure Re-Validation" section), staging has: `BilliardBarBackend`/`BilliardBarNginx`/`BilliardBarScheduler` confirmed Running with logs in `C:\POS\logs\`; all 16 D-07 secrets migrated to DPAPI (`C:\POS\secrets\`) and `AppEnvironmentExtra` repointed for Backend/Scheduler/TelegramBot; `BilliardBarTelegramBot` was `Stopped` due to a pre-existing crash-loop (`"The parameter is incorrect"`, NSSM/Application Event Log, predates Phase 3, never root-caused) — this plan is where that gets investigated; `BilliardBarPrintAgent` was never installed on staging at all (Phase 2 scope never covered it, since it normally runs on the physical bar host near a real printer).

`PS/Postgres` — the Postgres service name on staging is whatever `scripts/.postgres-service-name.txt` contains (auto-discovered at Phase 2 install time, e.g. something like `postgresql-x64-15` — do not assume the exact string, read the file).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy Phase 4's Wave-1 code to staging, install print-agent, fix/diagnose TelegramBot, apply Postgres failure-recovery, and capture live evidence</name>
  <files>N/A locally — this task runs commands on the staging Windows machine over SSH; it may modify scripts/install-nssm-telegram-bot.ps1 and/or telegram-bot/bot.py in this repo checkout ONLY if live diagnostics reveal a confirmed code-level bug</files>
  <read_first>
    .planning/phases/04-process-supervision-reliability-hardening/04-01-PLAN.md (exact backend/app/__init__.py changes: deepened health endpoint response shape, print-agent warn check, STEP 27 ghost-ticket triggers)
    .planning/phases/04-process-supervision-reliability-hardening/04-02-PLAN.md (exact backend/wsgi.py / backend/service_entry.py monkey-patch changes)
    .planning/phases/04-process-supervision-reliability-hardening/04-03-PLAN.md (scripts/check-health.ps1, scripts/configure-postgres-failure-recovery.ps1, and install-postgres-native.ps1's new call to it)
    .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md (staging path/scp method, current staging service state, and the exact pre-existing TelegramBot crash-loop symptom this task must diagnose)
    scripts/install-nssm-print-agent.ps1 (install steps for the print agent — runs fine without a physical printer attached; `win32print` auto-detect gracefully falls back to none-found rather than crashing)
    scripts/install-nssm-telegram-bot.ps1 (current AppStdout/AppStderr/AppDirectory/AppEnvironmentExtra config, to compare against live diagnostics)
  </read_first>
  <action>
    Source `STAGING_MACHINE_IP`/`STAGING_MACHINE_USERNAME`/`STAGING_MACHINE_PASSWORD` from the repo-root `.env` (never echo the password). Confirm connectivity with a trivial remote command first (`whoami`); if it fails with a timeout (not an auth error), ask the user to confirm the current `STAGING_MACHINE_IP` before proceeding (DHCP may have changed it since Phase 3).

    Push (via `scp`, staging is a file copy not a git clone) every file Plans 04-01/04-02/04-03 modified to their matching paths under `C:\Users\giris\billiards-staging\`: `backend/app/__init__.py`, `backend/wsgi.py`, `backend/service_entry.py`, `scripts/check-health.ps1`, `scripts/configure-postgres-failure-recovery.ps1`, `scripts/install-postgres-native.ps1`.

    Restart `BilliardBarBackend` via `nssm restart BilliardBarBackend` (or `Stop-Service`/`Start-Service` respecting the Nginx-dependency-order bug Phase 3's 03-05 fixed — stop Nginx first if needed, then Backend, then Nginx) so the health-endpoint/print-agent-check/DB-trigger changes take effect. `service_entry.py` runs `flask init-db` on every start, so STEP 27's trigger installation applies automatically — confirm its `"STEP 27: ghost-ticket structural invariant trigger installed"` line appears in `C:\POS\logs\backend.log` or `backend_err.log` after restart.

    Run `scripts\configure-postgres-failure-recovery.ps1` against the already-installed staging Postgres service, then `sc.exe qfailure <PgServiceName>` to confirm the policy applied.

    Install the print agent on staging for the first time via `scripts\install-nssm-print-agent.ps1` (no physical printer required for the service to start and respond on `/health`).

    Investigate `BilliardBarTelegramBot`'s pre-existing crash loop: pull the exact NSSM/Application Event Log entry (`Get-WinEvent -LogName Application -MaxEvents 15 | Where-Object {$_.Message -match 'BilliardBarTelegramBot|nssm'}` or `Get-EventLog -LogName Application -Source nssm -Newest 10`), then attempt a direct foreground run from an interactive SSH shell using the non-`w` `python.exe` in `telegram-bot\venv\Scripts\` against `telegram-bot\bot.py` so any real Python-level exception is visible on console (rather than swallowed by `pythonw.exe`'s windowless redirection). Apply the minimal fix indicated by that real error — candidates to check first: a stale/corrupted `telegram-bot\venv` (Python version mismatch since Phase 2 install), whether the DPAPI-sourced `AppEnvironmentExtra` values from Phase 3's 03-05 gap closure are correctly quoted/newline-free when NSSM passes them through, or a `python-telegram-bot`/`APScheduler` version incompatibility in `telegram-bot/requirements.txt`. Only modify the specific file(s) the live diagnostic actually points to (`telegram-bot/bot.py` and/or `scripts/install-nssm-telegram-bot.ps1`) — do not guess-and-patch without a confirmed root cause from the captured error. After any fix attempt, restart the service and confirm `Get-Service BilliardBarTelegramBot` reaches `Running` and stays running for at least 30 seconds without a new crash-loop event. If genuinely unfixable within this task's scope, record the exact diagnostic evidence and state plainly that it remains broken — never claim success without matching evidence.

    Capture and report back the literal command output for every item below:
    1. `Invoke-RestMethod http://localhost:5000/api/v1/health -TimeoutSec 5` while Postgres is running (expect `status: ok`, `db: connected`).
    2. `Stop-Service <PgServiceName>`, retry the same health call (expect an HTTP error / non-'ok' status, i.e. the deepened endpoint's 503 failure path), then immediately `Start-Service <PgServiceName>` and re-confirm the health call returns to `ok` — never leave Postgres stopped after this step.
    3. `Get-Content C:\POS\logs\backend_err.log -Tail 20` captured once with the print agent NOT yet running (expect the new `[WARN] Print agent unreachable...` line) and again once `BilliardBarPrintAgent` is confirmed `Running` (expect an `[OK]`/info line instead, no warning).
    4. A direct SQL query confirming both new triggers exist: `SELECT tgname FROM pg_trigger WHERE tgname LIKE 'trg_%consistency'` via `psql` — expect exactly 2 rows.
    5. A smoke test proving the new triggers do not break normal operation: log in via the API, open a ticket on any available resource, then close it via the normal `/close` flow — confirm both calls return 200/201, not a 500 or constraint-violation error.
    6. Full `powershell -File scripts\check-health.ps1` output.
    7. `sc.exe qfailure <PgServiceName>` output confirming the new failure-recovery policy.
    8. `Get-Service BilliardBarPrintAgent` showing `Running`.
    9. `Get-Service BilliardBarTelegramBot` status plus the real diagnostic finding and fix applied (or the diagnostic evidence and an honest "still broken" statement if unfixed).
  </action>
  <what-built>
    Backend code changes (deepened health check, print-agent warn check, ghost-ticket structural triggers, eventlet/psycopg2 monkey-patching) from Plans 04-01/04-02, plus two new PowerShell scripts (check-health.ps1, configure-postgres-failure-recovery.ps1) from Plan 04-03 — none of it has run on real Windows/Postgres/NSSM yet.
  </what-built>
  <how-to-verify>
    Run every step in this task's action on the actual staging machine (WIDOWSVAIL) as Administrator over SSH. Report back the 9 numbered captures verbatim.
  </how-to-verify>
  <acceptance_criteria>
    - All 6 files from Plans 04-01/04-02/04-03 were actually pushed to the real staging checkout and the backend was restarted to pick them up
    - The DB trigger existence query returned exactly 2 rows for `trg_%consistency`
    - The smoke-test ticket open/close cycle returned success codes, not a 500/constraint-violation error
    - `check-health.ps1`'s own output was captured and shows the correct per-service PASS/FAIL/WARN breakdown
    - `BilliardBarPrintAgent` is confirmed `Running` after this task, for the first time on staging
    - `BilliardBarTelegramBot`'s status is reported accurately — either genuinely fixed with the real root cause named, or still broken with concrete diagnostic evidence, never an unverified claim of success
    - Postgres was stopped and restarted exactly once (for the health-check-failure-path test) and confirmed running again before the task ends
  </acceptance_criteria>
  <verify>
    <human-check>Concrete command output matching every item in acceptance_criteria has been reported back (all 9 numbered captures) — not a bare "it worked" confirmation. If a step genuinely could not complete (unreachable/changed STAGING_MACHINE_IP, a script hang), the specific failing step and captured error are reported instead.</human-check>
  </verify>
  <done>Plans 04-01/04-02/04-03's code has been deployed and exercised on real staging hardware; print-agent is installed and running for the first time; TelegramBot's crash-loop status is accurately known (fixed or documented-broken); Postgres failure-recovery is applied and confirmed.</done>
  <resume-signal>Paste the requested command output above, or describe what failed and at which numbered step.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record validation results for NET-02, SUP-04, DATA-02, DATA-03, and partial SUP-01</name>
  <files>.planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 4's five Success Criteria)
    .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md (exact format/style to mirror: frontmatter, one section per requirement with a Status heading and quoted evidence, a final Summary table)
  </read_first>
  <action>
    Using Task 1's captured output (same plan), create `.planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md` with frontmatter (`phase`, `plan`, `validated` date, `scope: staging (WIDOWSVAIL) only`). Write one section each for NET-02, SUP-04, DATA-02, DATA-03, stating PASS/FAIL/PARTIAL with the specific evidence line from Task 1 backing each verdict (e.g. "SUP-04: PASS — Invoke-RestMethod returned status:ok/db:connected with Postgres up, and a non-'ok' response with Postgres stopped; check-health.ps1's full table is attached below."). Add a "SUP-01 (partial)" section documenting specifically the Postgres failure-recovery policy application, the print-agent install, and the TelegramBot fix/diagnosis from Task 1 — explicitly note this is partial and the full 6-service kill-isolation matrix continues in Plan 04-05. If Task 1's captured output shows any FAIL (a service not Running, a health-check failure, a smoke-test error, TelegramBot still broken), record that requirement as FAIL or PARTIAL with the specific reported error — do not mark it passing so `/gsd:verify-phase` can surface it as a gap. Leave a brief placeholder note that SUP-01 (full)/SUP-02/SUP-03 are covered by Plan 04-05 — do not fabricate their results here. Record a scope-confirmation line stating all execution happened on the staging machine only, and that D-06 was respected (no automated write/cleanup ran against the live bar machine's database anywhere in this phase).
  </action>
  <acceptance_criteria>
    - .planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md exists and contains sections for NET-02, SUP-04, DATA-02, DATA-03, and SUP-01 (partial)
    - each section's PASS/FAIL/PARTIAL status is backed by specific evidence quoted or paraphrased from Task 1's captured output, not asserted without evidence
    - any requirement whose Task 1 evidence showed a failure is recorded as FAIL/PARTIAL, not silently marked PASS
    - the document states this validation ran on staging only, and explicitly confirms D-06 compliance
  </acceptance_criteria>
  <verify>
    <automated>for id in "NET-02" "SUP-04" "DATA-02" "DATA-03" "SUP-01"; do grep -q "$id" .planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md || echo "MISSING:$id"; done | grep -c MISSING | grep -q "^0$" && echo PASS</automated>
  </verify>
  <done>.planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md exists and documents real staging-machine evidence for NET-02, SUP-04, DATA-02, DATA-03, and partial SUP-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Claude's SSH session to the staging machine | Arrives pre-elevated as Administrator — every command in this task runs at the highest privilege level available on that machine |
| This task's checkpoint output ↔ 04-VALIDATION.md | Captured SSH command output is trusted as-is when writing the validation record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Elevation of Privilege | Running multiple diagnostic/install/fix commands as pre-elevated Administrator over automated SSH | mitigate | Each individual script (check-health.ps1, configure-postgres-failure-recovery.ps1, install-nssm-print-agent.ps1) was already reviewed under its own plan's threat model (04-01/04-02/04-03) before being run here; Task 1 captures real output at every numbered step so a failure is caught immediately rather than compounding into the next step. |
| T-04-13 | Repudiation | 04-VALIDATION.md records SSH-captured output verbatim, with no independent second observer | accept | Same accepted limitation as Phase 2/3's 02-VALIDATION.md/03-VALIDATION.md precedent (T-02-19/T-03-15) — inherent to validating a Windows-only deployment target from a macOS planning/execution environment. |
| T-04-14 | Denial of Service | Deliberately stopping the Postgres service mid-task to test the health-check's failure path takes staging offline for the duration of that single step | mitigate | Staging only (D-08), never the live bar machine; Postgres is restarted immediately after the single health-check-failure observation, and the action text explicitly instructs "never leave Postgres stopped after this step." |
| T-04-15 | Tampering | Fixing the TelegramBot crash-loop based on live diagnostics without a confirmed root cause risks masking the real bug with an incorrect patch | mitigate | The action text explicitly forbids "guess-and-patch without a confirmed root cause" — any fix must trace to the actual captured Event Log entry or foreground-run exception. |
| T-04-16 | Information Disclosure | Diagnostic output (Event Log entries, a foreground bot.py run) could incidentally include `TELEGRAM_TOKEN`/`DATABASE_URL` in a traceback | mitigate | Acceptance criteria require reporting the diagnostic *finding* (error class/message), not a raw environment dump; if captured output unexpectedly contains a secret-looking value, that must be redacted before being recorded in 04-VALIDATION.md, mirroring 03-04-PLAN's T-03-16 precedent. |

Severity note: no HIGH-severity threat beyond what Plans 04-01/04-02/04-03 already addressed individually; this plan's own risk is procedural (verifying real execution and fixing a pre-existing bug), not a new attack surface.
</threat_model>

<verification>
Task 1 requires real staging-machine command output for all 9 numbered captures; do not mark this plan complete until 04-VALIDATION.md exists and contains real evidence (not placeholder text) for NET-02, SUP-04, DATA-02, DATA-03, and partial SUP-01.
</verification>

<success_criteria>
- Every file Plans 04-01/04-02/04-03 produced has actually run on the staging machine.
- The deepened health endpoint, print-agent warning, and ghost-ticket triggers are confirmed working against live Postgres/backend.
- BilliardBarPrintAgent is installed and Running on staging for the first time.
- BilliardBarTelegramBot's crash-loop status is accurately known (fixed with root cause, or documented-broken with evidence).
- `.planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md` records a PASS/FAIL/PARTIAL verdict with real evidence for NET-02, SUP-04, DATA-02, DATA-03, and partial SUP-01.
</success_criteria>

<output>
Create `.planning/phases/04-process-supervision-reliability-hardening/04-04-SUMMARY.md` when done
</output>
