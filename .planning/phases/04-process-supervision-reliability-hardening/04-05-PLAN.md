---
phase: 04-process-supervision-reliability-hardening
plan: 05
type: execute
wave: 3
depends_on: ["04-04"]
files_modified:
  - .planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md
autonomous: false
requirements: [SUP-01, SUP-02, SUP-03, SUP-04]

must_haves:
  truths:
    - "D-08/D-10: Each of the 6 services was individually force-killed (Stop-Process -Force, not nssm stop) on staging (WIDOWSVAIL only, never the live bar machine) and independently confirmed to auto-restart, with the other 5 services' PIDs proven unchanged throughout — not just 'still Running'"
    - "D-08/D-09/D-11: An actual Restart-Computer -Force was performed on staging (not a simulated stop/start), and all 6 services were confirmed to auto-start afterward in the correct DependOnService order (Postgres before Backend/Scheduler/TelegramBot, Backend before Nginx)"
    - "D-07: The final 04-VALIDATION.md contains a PASS/FAIL/PARTIAL verdict, backed by real evidence, for all 7 Phase 4 requirements — read-only diagnostic queries against the live bar machine remain out of scope for this phase"
  artifacts:
    - path: ".planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md"
      provides: "Complete evidence-backed record for SUP-01/02/03/04, NET-02, DATA-02, DATA-03"
      contains: "## Summary"
  key_links:
    - from: ".planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md"
      to: "ROADMAP.md Phase 4 Success Criteria (all 5)"
      via: "final Summary table, one row per requirement ID, each backed by staging evidence"
      pattern: "SUP-0[1-4]|NET-02|DATA-0[23]"
---

<objective>
Prove SUP-01 (independent per-service crash-restart), SUP-02 (correct dependency order at boot), and SUP-03 (auto-start after reboot/power loss) with live, real tests on the staging machine — a forced kill of each of the 6 services one at a time, and an actual `Restart-Computer -Force` — then compile the complete, final `04-VALIDATION.md` covering all 7 Phase 4 requirements.

Purpose: D-09/D-10 are explicit hard requirements that these three claims be proven with real process kills and a real reboot, not config review or a simulated stop/start — Docker's old all-or-nothing blast radius and Windows' boot-time dependency ordering can only be genuinely disproven or confirmed by actually doing it.

Output: Live crash-isolation and reboot evidence; the completed `.planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-process-supervision-reliability-hardening/04-CONTEXT.md
@.planning/phases/04-process-supervision-reliability-hardening/04-04-PLAN.md
@CLAUDE.md

<interfaces>
Staging machine access: same as Plan 04-04's `<interfaces>` block (root `.env` credentials, `sshpass`/`ssh` command pattern, DHCP-IP-may-have-changed caveat). By the time this plan runs, staging should already have: Plan 04-01/04-02/04-03's code deployed and restarted, Postgres failure-recovery applied, print-agent installed, and TelegramBot's crash-loop status known (per Plan 04-04's evidence) — read `04-04-SUMMARY.md` if it exists for the exact post-deploy state before starting.

`scripts/install-all-native-services.ps1` lines 550-566 — the existing `DependOnService` wiring this plan validates, not re-wires: `PostgreSQL → Backend/Scheduler/TelegramBot`, `Backend → Nginx`.

For a bounded remote-reachability poll after the reboot, use the Bash tool's `run_in_background`/Monitor until-loop pattern (e.g. an SSH retry loop with a bounded iteration count and short sleeps between attempts) rather than one long blocking foreground sleep.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Crash-isolation test — force-kill each of the 6 services independently (D-10, SUP-01)</name>
  <files>N/A — live staging test only</files>
  <read_first>
    .planning/phases/04-process-supervision-reliability-hardening/04-CONTEXT.md D-09/D-10/D-11 (the exact hard test method: `Stop-Process -Force` on the PID, not `nssm stop`/`Stop-Service`, and other services must NOT be manually touched during the test)
    .planning/phases/04-process-supervision-reliability-hardening/04-RESEARCH.md "Pitfall 3: Independent Service Crashes Interpreted as All-or-Nothing" (the specific mistake to avoid: manually restarting other services during the test, which would mask the isolation this test proves)
    scripts/check-health.ps1 (Plan 04-03 — use it to snapshot overall state before/after each kill cycle)
  </read_first>
  <action>
    For each of the 6 services — the Postgres service (name read from `scripts\.postgres-service-name.txt`), `BilliardBarBackend`, `BilliardBarScheduler`, `BilliardBarTelegramBot`, `BilliardBarNginx`, `BilliardBarPrintAgent` — one at a time: capture the current process ID for that service (e.g. `(Get-CimInstance Win32_Service -Filter "Name='<svc>'").ProcessId`), run `Stop-Process -Force -Id <pid>` (a hard kill, never `nssm stop`/`Stop-Service`/`Restart-Service` — this must exercise the actual OS-level crash-detection path, not a graceful shutdown), then poll `Get-Service <svc>` every few seconds for up to 30 seconds and confirm: (a) the killed service transitions back to `Running` with a NEW process ID within its configured restart delay (5 seconds for the 5 NSSM services; whatever delay `sc.exe qfailure` reports for Postgres), and (b) `Get-Service`/process-ID snapshots for the OTHER 5 services show uninterrupted `Running` status with UNCHANGED PIDs throughout the entire cycle — do not manually restart, stop, or otherwise touch any other service during this test. Run `powershell -File scripts\check-health.ps1` once after each individual kill+recovery cycle and capture its output. Record, for all 6 services, the before-PID, after-PID, and elapsed recovery time.
  </action>
  <what-built>N/A — this task exercises Plan 04-03's crash-restart configuration (NSSM AppExit Default Restart for 5 services, sc.exe failure for Postgres) live, for the first time.</what-built>
  <how-to-verify>Perform all 6 individual kill cycles on the real staging machine as Administrator over SSH, per this task's action. Report back all 6 before/after PID pairs, recovery times, and each cycle's check-health.ps1 output.</how-to-verify>
  <acceptance_criteria>
    - all 6 services were individually force-killed via `Stop-Process -Force` on their real PID (not a graceful stop)
    - each killed service is independently confirmed to have restarted with a NEW PID within its configured restart delay
    - for every single kill, the other 5 services' PIDs are proven UNCHANGED (PID continuity is the actual isolation proof — "still shows Running" alone is not sufficient)
    - no service was manually restarted by the operator/Claude during this test — only the intentionally-killed one recovered on its own
    - check-health.ps1 output was captured once per kill cycle
  </acceptance_criteria>
  <verify>
    <human-check>All 6 before/after PID pairs, recovery times, and per-cycle check-health.ps1 output have been reported, with explicit confirmation that the other 5 services' PIDs never changed during any single-service kill.</human-check>
  </verify>
  <done>All 6 services proven to crash-restart independently, with no cross-service blast radius, via real process kills.</done>
  <resume-signal>Paste the requested PID/recovery-time/check-health.ps1 evidence above, or describe what failed and for which service.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Reboot test — actual Restart-Computer -Force, confirm dependency-ordered auto-start (D-09/D-11, SUP-02, SUP-03)</name>
  <files>N/A — live staging test only</files>
  <read_first>
    .planning/phases/04-process-supervision-reliability-hardening/04-CONTEXT.md D-09/D-11 (real reboot required, not simulated; validates the existing DependOnService wiring, does not re-wire it)
    scripts/install-all-native-services.ps1 lines 550-566 (the exact DependOnService chain being validated)
  </read_first>
  <action>
    Record the current time and each of the 6 services' PID/StartTime before rebooting. Trigger `Restart-Computer -Force` on the staging machine over SSH (the SSH session will drop when the machine reboots — this is expected, not a failure). Poll for the machine coming back using a bounded retry loop (e.g. attempt a trivial `whoami` over SSH every 15-30 seconds, using the Bash tool's background/Monitor until-loop pattern rather than one long blocking foreground sleep) until connectivity returns or a reasonable timeout (e.g. 10 minutes) is reached — if it never comes back within a generous window, report this clearly rather than waiting indefinitely. Once reachable again, run `Get-Service <PgServiceName>,BilliardBar*` in a short polling loop (every 5-10 seconds for up to 3 minutes) and record the exact sequence/timestamps in which each of the 6 services reaches `Running`, to confirm Postgres becomes `Running` before Backend/Scheduler/TelegramBot, and Backend before Nginx (the existing `DependOnService` chain). Once the machine has been up for at least 60 seconds, run `powershell -File scripts\check-health.ps1` and capture its final PASS/FAIL output as proof all services are not just `Running` but actually responsive post-reboot.
  </action>
  <what-built>N/A — this task exercises Plan 02's DependOnService wiring and every NSSM service's SERVICE_AUTO_START flag under a real boot for the first time in this milestone.</what-built>
  <how-to-verify>Perform the actual reboot and post-reboot polling on the real staging machine, per this task's action. Report back the pre-reboot state, the reconnection timestamp, the observed per-service startup sequence with timestamps, and the final check-health.ps1 output.</how-to-verify>
  <acceptance_criteria>
    - an actual `Restart-Computer -Force` was issued (not a simulated stop/start sequence)
    - the machine's return-to-reachability was confirmed via active polling, not assumed after a fixed sleep
    - the observed startup ORDER is recorded with evidence (timestamps or a clear sequential Get-Service capture) showing Postgres before Backend/Scheduler/TelegramBot, and Backend before Nginx — not just a final all-Running snapshot
    - check-health.ps1's post-reboot output was captured and shows all services healthy
  </acceptance_criteria>
  <verify>
    <human-check>Pre-reboot state, reconnection timestamp, the ordered per-service startup evidence, and the final check-health.ps1 output have all been reported.</human-check>
  </verify>
  <done>A real reboot proves all 6 services auto-start in the correct dependency order with no manual intervention.</done>
  <resume-signal>Paste the requested reboot evidence above, or describe what failed (e.g. machine never came back, wrong startup order observed).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Compile the final Phase 4 validation record</name>
  <files>.planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 4's five Success Criteria — the exact claims this document must fully address)
    .planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md (Plan 04-04's existing partial sections — extend, do not overwrite)
    .planning/phases/03-centralized-logging-secrets/03-VALIDATION.md (the "## Summary" table format to mirror)
  </read_first>
  <action>
    Append SUP-01 (full — supersede Plan 04-04's partial SUP-01 section using Task 1's 6-service kill-isolation evidence from this same plan), SUP-02, and SUP-03 sections (using Task 2's reboot evidence) to the existing `04-VALIDATION.md`, each stating PASS/FAIL/PARTIAL with specific evidence. Add a final `## Summary` table covering all 7 Phase 4 requirements (SUP-01, SUP-02, SUP-03, SUP-04, NET-02, DATA-02, DATA-03) with one row each and a one-line evidence summary, mirroring `03-VALIDATION.md`'s summary-table format. Add a scope-confirmation line stating all execution across this entire phase happened on the staging machine (WIDOWSVAIL) only, and an explicit statement that D-06 was respected throughout — no automated write/cleanup action ran against the live bar machine's production database at any point in Phase 4.
  </action>
  <acceptance_criteria>
    - .planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md contains a `## Summary` table listing all 7 requirement IDs (SUP-01, SUP-02, SUP-03, SUP-04, NET-02, DATA-02, DATA-03)
    - SUP-01/SUP-02/SUP-03 sections are present, each backed by Task 1/Task 2 evidence from this plan
    - a scope-confirmation line explicitly states staging-only execution and D-06 compliance
  </acceptance_criteria>
  <verify>
    <automated>for id in "SUP-01" "SUP-02" "SUP-03" "SUP-04" "NET-02" "DATA-02" "DATA-03"; do grep -q "$id" .planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md || echo "MISSING:$id"; done | grep -c MISSING | grep -q "^0$" && grep -qi "D-06" .planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md && echo PASS</automated>
  </verify>
  <done>04-VALIDATION.md is the complete, evidence-backed record for all 7 Phase 4 requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Claude's SSH session to the staging machine | Arrives pre-elevated as Administrator; this plan issues the highest-impact commands in the phase (`Restart-Computer -Force`, repeated `Stop-Process -Force`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Denial of Service | Force-killing every service one at a time, plus a full machine reboot, intentionally takes staging offline repeatedly during this plan | accept | Staging is a disposable validation environment by design (CLAUDE.md), never the live bar machine (D-08); this is the explicit, required test method for SUP-01/02/03 per D-09/D-10 — the outage IS the test. |
| T-04-18 | Repudiation | Same repudiation limitation as prior phases (T-02-19/T-03-15/T-04-13) — no independent second observer of the SSH-captured evidence | accept | Inherent to remote SSH-driven validation from a non-Windows planning environment; consistent accepted precedent across all three prior validation plans. |
| T-04-19 | Elevation of Privilege | `Restart-Computer -Force` and `Stop-Process -Force` are high-impact Administrator-level commands run via automated SSH | mitigate | Scoped exclusively to the staging machine's credentials (`STAGING_MACHINE_*` env vars) per D-08 — this plan never references or uses the separate `BAR_MACHINE_*` production credentials. |

Severity note: no HIGH-severity threat beyond the accepted, deliberate staging outages this test method requires; the live bar machine's credentials/IP are never referenced in this plan.
</threat_model>

<verification>
Both live-test tasks require real staging-machine evidence (PIDs, timestamps, check-health.ps1 output); do not mark this plan complete until 04-VALIDATION.md's final Summary table shows real evidence for all 7 Phase 4 requirements.
</verification>

<success_criteria>
- All 6 services proven to crash-restart independently via real `Stop-Process -Force` kills, with PID-level proof the other 5 were never touched.
- A real `Restart-Computer -Force` proves correct dependency-ordered auto-start of all 6 services.
- `.planning/phases/04-process-supervision-reliability-hardening/04-VALIDATION.md` contains a complete, evidence-backed Summary table for all 7 Phase 4 requirements, with explicit D-06 compliance confirmation.
</success_criteria>

<output>
Create `.planning/phases/04-process-supervision-reliability-hardening/04-05-SUMMARY.md` when done
</output>
